Домашнее задание выполнено на локальной машине (macOS).
Jenkins развернут, плагины (Sonar, Allure, Nexus) установлены, инструменты (JDK 17, Maven) настроены.

Пайплайны отрабатывают корректно до момента подключения к внешним сервисам (SonarQube, Nexus), так как этих серверов нет в локальном окружении. Ошибки Connection refused в логах подтверждают, что скрипты пытаются выполнить требуемые действия.
